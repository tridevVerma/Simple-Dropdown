import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

// Get root id and render App component
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
